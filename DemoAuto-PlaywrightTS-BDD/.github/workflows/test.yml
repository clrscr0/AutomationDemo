name: Playwright Test
run-name: ${{ github.actor }} is running Playwright tests on GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
        - name: Run test
          run: npm test
        - run: echo "🍏 This job's status is ${{ job.status }}."